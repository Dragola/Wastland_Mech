using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * On hand crafting (limited)
 * Crafting table/bench V1, V2, etc?
 * 
 * On hand crafting = check player inventory
 * Crafting table/bench = base + player inventory + mech (if docked or have mech storage auto empty [could make option])?
 */
public class Crafting : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
